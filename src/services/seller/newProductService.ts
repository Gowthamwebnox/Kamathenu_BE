import { PrismaClient } from "../../generated/prisma/client";

const prisma = new PrismaClient();

export const newProductService = async ():Promise<any> => {
    try {
        
    } catch (error) {
        
    }
};